// Tables are now responsive with simple overflow-x: auto
// No synchronization needed - CSS handles all responsive behavior
export function initializeTableScroll(): void {
  // Keep for backward compatibility - all logic moved to CSS
}
